@model IEnumerable<ClinicManagement.Models.Usertype>

@{
    ViewBag.Title = "Index";
}
<div class="row">
    <div class="panel-body">
        <div class="alert alert-info">
            <p style="color:black;font-size:16px;">User Types</p>
        </div>
        </div>
</div>

        <p>
            <a href="@Url.Action("Create","UserTypes")"><b class="btn btn-default">Create New</b></a>
        </p>
        <table class="table table-hover table-striped">
            <tr>
                <th>
                    @Html.DisplayNameFor(model => model.Name)
                </th>
                <th></th>
            </tr>

            @foreach (var item in Model)
            {
                <tr>
                    <td>
                        @Html.DisplayFor(modelItem => item.Name)
                    </td>
                    <td>
                      
                     
                       
                    </td>
                </tr>
            }

        </table>
